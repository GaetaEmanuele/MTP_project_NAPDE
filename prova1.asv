clear all
clc
close all

[sol,nodes] = c_main();


 uex = @(x,t) sin(pi*x).*cos(pi*t);
 x = nodes(1,:);
 t = zeros(size(sol,1),1);
 for i=2:size(sol,1)
    if mod(i,2)==0
        t(i-1) = nodes(i,1);
    else
        t(i-1) = nodes(i,2);
    end
 end
 t(end) = 2;
 u = uex